# Marketplace API origin. Production checkout fails closed when this is absent.
NEXT_PUBLIC_MARKETPLACE_API_URL=http://localhost:4100

# Seeded catalog and non-transactional checkout are development-only.
NEXT_PUBLIC_DEMO_MODE=true
